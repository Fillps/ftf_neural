[DEFAULT]

debug = False

# Name of the gdb executable, usualy is "gdb-mic" for Xeon Phi
gdbExecName = gdb

# How many interrupt signals to send sequentially
seqSignals = 5
# How many random threads will attempt to send interrupt signals
numThreadsFI = 3

# Max time to send signal. It should be the app exec time in seconds
maxWaitTime = 10

# Commands to set the session inside GDB environment
gdbInitStrings = file /tmp/neural/neural;set args > output

goldFile = /tmp/neural/gold
outputFile = /tmp/neural/output

# Shell command to send SIGINT to the application being tested
signalCmd = killall -2 neural

# Kill commands to exit all applications (GDB and the application tested)
# Need to ensure safe termination before another fault injection test
killStrs = killall -9 gdb;killall -9 neural


[neural-single]
# Start and end time to try randomly inject a fault
initSignal = 1
endSignal = 3
# Which fault model to use, 0 -> single; 1 -> double; 2 -> random; 3 -> zeros; 4 -> least significant bits (LSB)
faultModel = 0

[neural-single2]
# Start and end time to try randomly inject a fault
initSignal = 2
endSignal = 4
# Which fault model to use, 0 -> single; 1 -> double; 2 -> random; 3 -> zeros; 4 -> least significant bits (LSB)
faultModel = 0

[neural-double]
# Start and end time to try randomly inject a fault
initSignal = 1
endSignal = 3
# Which fault model to use, 0 -> single; 1 -> double; 2 -> random; 3 -> zeros; 4 -> least significant bits (LSB)
faultModel = 1


[neural-double2]
# Start and end time to try randomly inject a fault
initSignal = 2
endSignal = 4
# Which fault model to use, 0 -> single; 1 -> double; 2 -> random; 3 -> zeros; 4 -> least significant bits (LSB)
faultModel = 1

[neural-lsb]
# Start and end time to try randomly inject a fault
initSignal = 2
endSignal = 4
# Which fault model to use, 0 -> single; 1 -> double; 2 -> random; 3 -> zeros; 4 -> least significant bits (LSB)
faultModel = 4

[neural-lsb2]
# Start and end time to try randomly inject a fault
initSignal = 1
endSignal = 3
# Which fault model to use, 0 -> single; 1 -> double; 2 -> random; 3 -> zeros; 4 -> least significant bits (LSB)
faultModel = 4

[neural-random]
# Start and end time to try randomly inject a fault
initSignal = 0
endSignal = 1
# Which fault model to use, 0 -> single; 1 -> double; 2 -> random; 3 -> zeros; 4 -> least significant bits (LSB)
faultModel = 2

[neural-random2]
# Start and end time to try randomly inject a fault
initSignal = 2
endSignal = 3
# Which fault model to use, 0 -> single; 1 -> double; 2 -> random; 3 -> zeros; 4 -> least significant bits (LSB)
faultModel = 2

[neural-zeros]
# Start and end time to try randomly inject a fault
initSignal = 1
endSignal = 2
# Which fault model to use, 0 -> single; 1 -> double; 2 -> random; 3 -> zeros; 4 -> least significant bits (LSB)
faultModel = 3

[neural-zeros2]
# Start and end time to try randomly inject a fault
initSignal = 3
endSignal = 4
# Which fault model to use, 0 -> single; 1 -> double; 2 -> random; 3 -> zeros; 4 -> least significant bits (LSB)
faultModel = 3
